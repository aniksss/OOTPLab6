public class CarCatalog
{
    public static void Update(List<Car> cars)
    {
        Console.WriteLine($"Updating CarCatalog with {cars.Count} cars.");
        // update the UI with the filtered cars
    }
}